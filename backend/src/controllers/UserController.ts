

/**
 * Authenticates a user.
 */
const authenticate = (req: Request, res: Response) => {
  /*const { user, password } = res.body
  if (!user || !password) {

  }
  res.json()*/
}

export {authenticate}